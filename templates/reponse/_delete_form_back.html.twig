{% extends 'back.html.twig' %}






{% block body4 %}
    <form method="post" action="{{ path('reponse_delete_back', {'id': reponse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_method" value="DELETE">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse.id) }}">
        <button class="btn">Supprimer</button>
    </form>

{% endblock %}

{% block body5 %}
{% endblock %}

